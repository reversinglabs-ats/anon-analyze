# Base URL - no quotes, no trailing slash
# Example: https://your.appliance.reversinglabs.com
ANALYZE_API_BASE=https://your.appliance.reversinglabs.com

# API token - must be exactly 40 hexadecimal characters
# Get this from your Spectra Analyze instance under Administration > Tokens
ANALYZE_API_TOKEN=your40characterhexadecimalapitokenhere

# SSL verification for requests to the Analyze API (outbound connections).
# Set to "false" to disable SSL certificate verification when connecting to
# Analyze instances with self-signed certificates.
# Default: true (SSL verification enabled)
# WARNING: Only disable in trusted network environments.
# ANALYZE_SSL_VERIFY=true

# TLS/HTTPS mode for incoming connections to this application.
# Set to "true" to enable HTTPS mode.
# Default: false (HTTP mode)
# ENABLE_TLS=false

# Custom TLS certificate paths (optional).
# If ENABLE_TLS=true and these are not set, a self-signed certificate is auto-generated.
# Paths are from the container's perspective when running in Docker.
# TLS_CERT_PATH=/certs/cert.pem
# TLS_KEY_PATH=/certs/key.pem
